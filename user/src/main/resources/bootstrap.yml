server:
  port: 518
spring:
  application:
    name: user
  cloud:
    nacos:
      config:
        file-extension: yaml
        group: DEFAULT_GROUP
        namespace: luo
        prefix: ${spring.application.name}
        server-addr: 127.0.0.1:8848